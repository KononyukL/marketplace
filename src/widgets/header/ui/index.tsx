import { Head } from "@/widgets/header/ui/head";
import { Menu } from "@/widgets/header/ui/menu";

export const Header = () => {
  return (
    <header>
      <Head />
      <Menu />
    </header>
  );
};
